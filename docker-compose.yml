version: '3.8'

services:
  database:
    image: mongo
    container_name: mongo-3
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db


  backend:
    image: ${BACKEND_IMAGE}
    container_name: backend
    ports:
      - "5002:5000"
    depends_on:
      - database
    env_file:
      - ./backend/.env


  frontend:
    image: ${FRONTEND_IMAGE} 
    container_name: frontend
    ports:
      - "3002:3000"
    depends_on:
      - backend
volumes:
  mongo-data:
